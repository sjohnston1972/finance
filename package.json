{
  "name": "pension-planner",
  "version": "1.0.0",
  "private": true,
  "description": "UK Pension & Salary Planner on Cloudflare Workers",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.110.0"
  },
  "devDependencies": {
    "wrangler": "^4.110.0"
  }
}
